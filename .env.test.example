# Use a dedicated throwaway database for destructive test runs.
TEST_DATABASE_URL=postgres://user:pass@localhost/ledger_one_test

# For a remote isolated test database, export this in your shell only.
# Do not put it in .env.test.
# export LEDGER_ONE_ALLOW_DESTRUCTIVE_TEST_DB='<host>/<dbname>'
