# Use a dedicated throwaway database for destructive test runs.
TEST_DATABASE_URL=postgres://user:pass@localhost/ledger_one_test

# Required only if TEST_DATABASE_URL points to a non-local isolated test database.
# LEDGER_ONE_ALLOW_DESTRUCTIVE_TEST_DB=1
